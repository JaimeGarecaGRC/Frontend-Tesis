import React from 'react';

const AppArrayContext = React.createContext(null);

export default AppArrayContext;

/*
const [state, setState] = useState(initialState);

const changeData = (payload) => {
    setState(payload)
}

*/
